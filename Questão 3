#include <stdio.h>
    int main (){
    int num,impar=1, i;
    printf("Insira um numero:");
    scanf("%d",&num);
    for ( i = 1; i <= num && i >0; i++)
    { 
        printf("  %d  ",impar);
        impar = impar +2;
    }
	}
